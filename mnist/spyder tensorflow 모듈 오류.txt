anaconda prompt에서
1. pip install tensorflow하여 tensorflow 받기
2. conda install tensorflow. 위 방법이 되지 않을시 사용.
	- 버전에 따라 되지 않을 수 도 있기때문
  버전 오류일시
	- conda update --all
	- conda update -n base conda
	- conda install -c anaconda setuptools